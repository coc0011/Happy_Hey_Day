#! bin/bash

#ask the user to enter their name
echo "What is your name?"

#instruct the computer to interpret the input as a variable called name
read name

#print this statement back to the user using the name variable
echo "Hey, $name!"
